language: rust
rust:
  - nightly
#  - 1.0.0-beta
sudo: false
cache:
  directories:
    - $HOME/.cargo
    - $HOME/rust
    - $HOME/rust-installer

script:
  - cargo build --verbose
  - cargo test --verbose
  - cargo doc --verbose
  - mv target/doc doc
